package SuperMarketSimulation.Classes;

/*
This is my enumerated type class which allows any instances of Product I create use a enum type
such as chicken breast would use RAWMEAT, this is then used to create categories using checkboxes,
any enums added to the class do not have to be manually created in the GUI a method does this already
 */
public enum ProductType {
    RAWMEAT,
    FRUITANDVEG,
    DRINKS,
    HOUSEHOLD,
    COOKEDMEAT,
    FASHION
}
